#### 參數越多，訓練時觀察到的 Loss 跟測試時觀察到的 Loss 會差距越大

> More parameters, easier to overfit.

#### Pokemon/ Digimon Classifier 

​	We want to find a function . . .

​	Determine a function with unknown parameters ( based on domain knowledge )

​	$f_h$ : function with threshold $h$

​	所有 $h$ 集合起來叫做 $H$ = { 1, 2, $\cdots$ , 10,000 }

​	$|H|$: 有多少可能的選擇，稱作 model "complexity"

​	定義完有未知數的 function 後，再來定義 $Loss$ 

​	$Loss$ function (given data)
$$
D = \{(x^1, \hat{y}^1), (x^2, \hat{y}^2), \cdots , (x^N, \hat{y}^N)\}
$$
​	Loss of a threshold $h$ given data set $D$

#### 	Error rate : 

$$
L(h, D) = \frac{1}{N}\sum\limits_{n=1}^Nl(h, x^n, \hat{y}^n)	
$$

$$
\text{if} f_h(x^n) \neq \hat{y}^n \\
​		&\text{output 1}\\
​	\text{otherwise }\\
​		&\text{output 0}\\
$$

---

#### Training Examples

​	理想：$h^{all} = arg min L(h, D_{all})$

​	現實：$h^{train} = arg minL(h, D_{train})$

We hope $L(h^{train}, D_{all})$ and $L(h^{all}, D_{all})$ are close.

---

#### What do we want

- 目標：
  - $L((D_{train}), D_{all}) - L(h_{all}, D_{all}) ≤ δ$
  - → Generalization gap (泛化誤差) 小於 $δ$。
- 條件：
  - 只要滿足：$∀ h ∈ 𝓗$ (Hypothesis set, 假設集合)，$|L(h, (D_{train})) − L(h, D_{all})| ≤ ε (ε = δ/2)$
  - → 對所有可能模型 $h$，訓練資料 $(D_{train})$ 和全部資料 $(D_{all})$ 的 $Loss$ 差距不超過 $ε$。

##### 推導過程：

- $L((h_{train}), D_all) ≤ L(h_{train}, (D_{train})) + δ/2$
- $L(h_{train}, (D_{train})) ≤ L(h_all, (D_{train}))$（因為 $h_{train}$ 是 $arg min L(h, (D_{train}))$）
- $L(h_all, (D_{train})) ≤ L(h_{all}, D_{all}) + δ/2$
- 合起來：$L(h_{train}, D_{all}) − L(h_{all}, D_{all}) ≤ δ$

---

#### 和的理解 (Union Bound)

- $P(D_{train}$ is bad$)=⋃h∈HP$($D_{train}$ is bad due to $h$)
- $≤∑h∈HP(D_{train}$ is bad due to $h)$
- $≤∣H∣×2exp⁡(−2Nε2)$
- **視覺圖示**：
  - 每個 h 對應一群可能發生 bad 的情況（如橘色點），用不同框框代表不同 h。
  - 雖然實際上是聯集 (Union)，但為了方便上界估計，用加總 (Sum) 來計算。這是 Union Bound 的概念。

---

#### Hoeffding's Inequality (霍夫丁不等式) 應用

- **公式**：
  - $P(D_{train}$ is bad due to $h)≤2exp⁡(−2Nε2)$
- **補充**：
  - Loss L 範圍是 [0,1]。
  - N 是 $D_{train}$ 裡面的樣本數 (Number of training examples)。

---

#### 如何讓 P($D_{train}$ is bad) 變小？

- **兩個方向**：
  - 增加 N（樣本數 Number of training examples）
  - 減少 $∣H∣$（模型複雜度 Model complexity）
- **但要注意**：
  - $∣H∣$ 不能隨便縮小，因為可能會導致模型無法 fit 資料 → 理想崩壞 (Ideal collapse)。

---

#### 模型複雜度與樣本數的 Tradeoff (取捨)

- Larger N 和 Smaller $∣H∣$ → $L(h_{train},D_{all})−L(h_{all},D_{all})≤δ$
- Smaller $∣H∣$ → Larger $L(h_{all},D_{all})$
- 圖示解釋：
  - $∣H∣$ 大時，$h_{all}$ 可以找到更小的 Loss。
  - $∣H∣$ 小時，選擇比較少，$h_{all}$ 的 Loss 可能比較大。

---

#### 範例計算 (Example Calculation)

- $∣H∣$=10,000，$δ$=0.1，$ε$=0.1
- $N≥\frac{log⁡(2×\frac{∣H|}{δ})}{2ε^2}$
- 實際數字：
  - $∣H∣$=10,000，N=100，$P(D_{train}$ is bad$) \leq$2707 → 很糟。
  - $∣H∣$=10,000，N=500，$P(D_{train}$ is bad$)≤$0.91 → 仍然太大。
  - $∣H∣$=10,000，N=1000，$P(D_{train}$ is bad$)≤$0.00004 → 可接受。

----

#### 核心結論 (Summary)

- **模型越複雜 (Larger Model Complexity $∣H∣$)** → 越容易 overfit → 需要更多資料 (Larger N)。
- **要平衡模型複雜度與資料量**：
  - 不希望模型太簡單 (Too simple)，也不希望太複雜 (Too complex) 而資料量不足 (Insufficient data)。